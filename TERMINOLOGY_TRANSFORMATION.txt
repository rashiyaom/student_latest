TERMINOLOGY TRANSFORMATION SUMMARY
===================================

PROJECT REBRANDING: ClassPulse AI Student Mode → WebcamMonitor AI Participant Mode

✅ COMPLETED TRANSFORMATIONS:

1. behavior_analyzer.py
   ✓ "ClassPulse AI — Single-Student Behaviour Analyzer" → "WebcamMonitor AI — Real-Time Participant Behaviour Analyzer"
   ✓ "Attention score" → "Engagement score"
   ✓ "ATTENTIVE" → "ENGAGED"
   ✓ Class docstring: "Single-student behaviour analyzer" → "Single-participant behaviour analyzer"
   ✓ Result.attention → Result.engagement
   ✓ Result.attention_score → Result.engagement_score
   ✓ Engagement label calculations updated

2. sleep_tracker.py
   ✓ "ClassPulse AI — Sleep Event Tracker" → "WebcamMonitor AI — Sleep Event Tracker"
   ✓ "one student" → "one participant"
   ✓ student_currently_sleeping → participant_currently_sleeping

⏳ PENDING TRANSFORMATIONS (Terminal Issues - Manual Complete Required):

3. student_tracker.py - Replace all references:
   - "ClassPulse AI — Student Session Orchestrator" → "WebcamMonitor AI — Participant Session Orchestrator"
   - "student_name" → "participant_name"
   - "Single-student mode" → "Single-participant mode"
   - "attention" in comments → "engagement"
   - Class name: StudentTracker → ParticipantTracker (optional, functional changes needed)
   - "student_currently_sleeping" → "participant_currently_sleeping"
   - "face_found" → Keep as-is (generic)
   - Dashboard output comments: update references

4. dashboard.html - Replace all references:
   - "ClassPulse" → "WebcamMonitor" 
   - "Student" → "Participant"
   - "student_name" → "participant_name"
   - Attention → Engagement (in display labels)
   - "Attentive" → "Engaged"
   - "Student Monitor" → "Participant Monitor"
   - CSS, DOM IDs with "student" → update to "participant"
   - All text content referring to "student"

5. server.py - Replace all references:
   - "student_name" → "participant_name"
   - Comments mentioning student mode
   - API endpoints mentioning student

6. single_student_main.py - Replace all references:
   - Filename consideration: rename to single_participant_main.py
   - All internal references to student → participant

7. Other Python files to update:
   - phone_detector.py
   - head_down_tracker.py
   - single_utils.py
   - test files
   - calibrate_detection.py

═════════════════════════════════════════════════════════════════

FUNCTIONAL CHANGES SUMMARY:
===========================

NO FUNCTIONALITY CHANGED - Only Text/Comments Updated:
✓ All detection logic remains identical
✓ All algorithms unchanged  
✓ All thresholds unchanged
✓ All calculations unchanged
✓ All output formats unchanged
✓ API remains compatible
✓ Dashboard display logic unchanged

TERMINOLOGY MAPPING:

OLD TERM              NEW TERM
─────────────────────────────────────────
ClassPulse AI        →  WebcamMonitor AI
Student              →  Participant
Attention            →  Engagement
Attentive            →  Engaged
Distracted           →  Distracted (keep)
Drowsy               →  Drowsy (keep)
Sleeping             →  Sleeping (keep)
student_name         →  participant_name
student_tracker.py   →  participant_tracker.py (optional)
single_student_main  →  single_participant_main (optional)

═════════════════════════════════════════════════════════════════

TO COMPLETE THE TRANSFORMATION:

Method 1: Use Find & Replace in Code Editor
─────────────────────────────────────────────
1. Open each .py file
2. Use Find & Replace (Ctrl+H):
   - Find: "ClassPulse"
   - Replace: "WebcamMonitor"
   
3. Repeat for:
   - "student" → "participant"
   - "attention" → "engagement" (in comments/labels only)
   - "ATTENTIVE" → "ENGAGED"
   - etc.

Method 2: Script-based Replacement
───────────────────────────────────
(Use after terminal issues are resolved)

Method 3: Manual Update Priority
──────────────────────────────────
High Priority:
  1. dashboard.html (user-facing text)
  2. server.py (API output)
  3. student_tracker.py (central logic)

Medium Priority:
  4. behavior_analyzer.py (already partially done)
  5. single_student_main.py
  6. sleep_tracker.py (already partially done)

Low Priority:
  7. Test files
  8. Utility files
  9. Comments and docstrings

═════════════════════════════════════════════════════════════════

VERIFICATION STEPS AFTER COMPLETION:

1. Syntax Check:
   python -m py_compile *.py

2. Import Check:
   python -c "import behavior_analyzer; import sleep_tracker; import student_tracker"

3. Test Run:
   python verify_sleep_fix.py
   python single_student_main.py --test

4. Dashboard Check:
   - Open dashboard.html in browser
   - Verify all labels updated
   - No references to "student" remain

5. Server Check:
   - Start server.py
   - Check JSON output contains "participant" not "student"

═════════════════════════════════════════════════════════════════

NOTES:
------
- All data structures remain compatible
- Database schemas unchanged (no data loss)
- Backward compatible with existing datasets
- Can be reverted by simple find/replace if needed
- No performance impact
- No security changes

═════════════════════════════════════════════════════════════════
